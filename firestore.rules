rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cho phép đọc/ghi dữ liệu dùng chung và đồng bộ tức thì cho các máy
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
